[package]
name = "shutil-which"
description = "Crossplatform which"
license = "MIT"
version = "0.1.6"
edition = "2024"
publish = false

[dependencies]
clap = { workspace = true, features = ["derive"] }
which.workspace = true

[[bin]]
path = "main.rs"
name = "which"
